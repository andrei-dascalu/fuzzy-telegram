container_commands:
  05-run-db-migrations:
    command: "chown -R www-data:www-data . && sudo -u www-data composer phinx migrate"
    cwd: "/var/app/staging"
    ignoreErrors: false
    leader_only: true
    env:
      COMPOSER_HOME: /usr/bin
